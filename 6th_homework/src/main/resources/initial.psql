CREATE SCHEMA IF NOT EXISTS zoo;


CREATE TABLE IF NOT EXISTS zoo.animal(
    animal_id SERIAL PRIMARY KEY NOT NULL,
    animal_name VARCHAR(256) UNIQUE NOT NULL,
    animal_classes VARCHAR(256) NOT NULL,
    animal_eating_type VARCHAR(256) ,
    animal_skin_type VARCHAR(256)
);

TRUNCATE TABLE zoo.animal RESTART IDENTITY;